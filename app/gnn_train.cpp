// gnn_train.cpp -- the must-ship deliverable: node classification on the
// Elliptic Bitcoin dataset.
//
// Loads the dataset (via the data-loading pieces Teammate A owns), builds
// the GNN model from engine ops, trains with Adam, evaluates against the
// published ~95% AUROC baseline, and (week 4) runs the MLP-vs-GNN ablation
// by toggling neighbor_aggregation off.
//
// If this file stays short, that's the engine doing the real work --
// which is the point.

#include "microtorch/Tensor.hpp"

int main() {
    // TODO: load Elliptic data, build model, train, evaluate, ablate.
    return 0;
}
