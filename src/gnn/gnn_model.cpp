#include "microtorch/Tensor.hpp"

namespace microtorch {

// TODO: node-classification GNN forward pass (matmul + neighbor_aggregation
// + ReLU stacked, then softmax_cross_entropy). Must-ship deliverable.

}  // namespace microtorch
