#include "microtorch/Tensor.hpp"

namespace microtorch {

// TODO: MLP denoiser + noise schedule + reverse sampling on 2D toy data.
// Stretch goal -- guaranteed attempt per the week 3 checkpoint, fallback
// to autoencoder only if this doesn't pan out.

}  // namespace microtorch
