#TODO: Implement a single function that runs the forward and backward once to get analytic gradient
#runs the forward only many times nudging one input lements ata a time to build numeric gradient
#return how far apart the gradient from engine/ops and engine/harness